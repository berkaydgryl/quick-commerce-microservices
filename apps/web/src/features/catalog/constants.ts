/** Katalog ozelligi sabitleri (ADR-11). */

/** Kategori listesi seyrek degisir; bu sure icinde yeniden istenmez. */
export const CATEGORIES_STALE_TIME_MS = 5 * 60 * 1000;

/** Kategoriler yuklenirken gosterilen iskelet kutucugu sayisi. */
export const CATEGORY_SKELETON_COUNT = 5;

/** Market urun listesinde bir sayfada istenen urun sayisi (sunucu sinirlar icinde kirpar). */
export const MARKET_PRODUCTS_PAGE_SIZE = 20;
